<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_chat_room"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.vivekbalachandra.android.ima.ChatRoomActivity">

    <TextView
        android:id="@+id/ph"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="clicked at postion : " />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:text="keep calm until chat is implemented"
        android:textSize="40dp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/message_box"
        android:layout_width="300dp"
        android:layout_marginLeft="20dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="10dp" />

    <ImageView
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignBottom="@+id/message_box"
        android:layout_marginLeft="10dp"
        android:layout_marginStart="1dp"
        android:layout_toEndOf="@+id/message_box"
        android:layout_toRightOf="@+id/message_box"
        android:src="@drawable/send" />
</RelativeLayout>
